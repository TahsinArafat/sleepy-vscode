// sleepy_change - new file in Sleepy-owned directory
//
// Shared constants for the Sleepy Code integration.

/** Default Sleepy API URL (local development). */
export const SLEEPY_API_URL_DEFAULT = "http://localhost:3000"

/** Environment variable that holds the Sleepy virtual key. */
export const SLEEPY_ACCESS_TOKEN_ENV = "SLEEPY_ACCESS_TOKEN"

/** The well-known provider ID used in config files (opencode.jsonc / kilo.jsonc). */
export const SLEEPY_PROVIDER_ID = "sleepy"
